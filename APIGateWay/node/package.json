{
  "name": "apigateway",
  "version": "1.0.0",
  "description": "nginx API Gateway",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "docker_build": "docker build -t api_gateway -f Dockerfile .",
    "docker_run": "docker run -d --rm --name api_gateway -p 5000:5000 api_gateway_1",
    "docker_run_dev": "./bin/run_dev.sh"
  },
  "keywords": [
    "nginx",
    "api",
    "gateway"
  ],
  "author": "Nam Phan",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.24.0",
    "express": "^4.17.1"
  }
}
